# ──────────────────────────────────────────────────────────────────────────────
# Algorithms-Engine  –  Environment Variables
# Copy this file to .env and fill in the values.
# ──────────────────────────────────────────────────────────────────────────────

# (Required) Base URL of the server that should receive scheduling results.
# The engine will POST the full result payload to this URL automatically.
CALLBACK_SERVER_URL=http://51.20.95.111:3500

# (Optional) Host and port for uvicorn (defaults shown)
HOST=0.0.0.0
PORT=8000
